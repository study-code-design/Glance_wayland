{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default-capability",
  "description": "Allow local app windows to use Tauri core APIs.",
  "windows": ["main", "overlay", "capture"],
  "permissions": ["core:default"]
}
